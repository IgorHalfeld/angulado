
let observer = {
  name: 'observer'
}

//   // Through each property to turn reactive
//   // ======================================
//   observe (obj) {
//     for (let key in obj) {
//       if (obj.hasOwnProperty(key)) this.reactive(obj, key)
//     }
//   }
